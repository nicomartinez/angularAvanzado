import { FavoriteTextPipe } from './favorite-text.pipe';

describe('FavoriteNextPipe', () => {
  it('create an instance', () => {
    const pipe = new FavoriteTextPipe();
    expect(pipe).toBeTruthy();
  });
});
